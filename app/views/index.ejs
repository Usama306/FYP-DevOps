<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= environment.toUpperCase() %> Environment</title>
    <link href="/styles.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
</head>
<body class="min-h-screen relative overflow-hidden bg-gradient-to-br from-gray-900 to-gray-800">
    <!-- Particles container -->
    <div id="particles-js" class="absolute inset-0 z-0"></div>

    <!-- Content -->
    <div class="relative z-10 container mx-auto px-4 py-8 min-h-screen flex items-center justify-center">
        <% if (environment === 'development') { %>
            <div class="bg-blue-500/20 backdrop-blur-lg rounded-xl shadow-2xl p-12 text-white border border-blue-400/30 max-w-2xl w-full transform hover:scale-105 transition-all duration-300">
                <div class="text-center space-y-6">
                    <div class="inline-block p-3 rounded-full bg-blue-500/30 mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                        </svg>
                    </div>
                    <div class="bg-blue-600/30 px-4 py-2 rounded-full inline-block mb-4">
                        <span class="text-lg font-mono">NODE_ENV: <%= environment %></span>
                    </div>
                    <h1 class="text-5xl font-bold mb-4 bg-gradient-to-r from-blue-200 to-blue-400 text-transparent bg-clip-text">
                        Development Environment
                    </h1>
                    <p class="text-xl text-blue-100 leading-relaxed">
                        Welcome to the development environment. This is where you build and test new features.
                    </p>
                    <div class="flex justify-center gap-4 mt-8">
                        <button class="px-6 py-3 bg-blue-500 hover:bg-blue-600 rounded-lg font-semibold transition-colors duration-200">
                            Get Started
                        </button>
                        <button class="px-6 py-3 bg-transparent border border-blue-400 hover:bg-blue-500/20 rounded-lg font-semibold transition-colors duration-200">
                            Documentation
                        </button>
                    </div>
                </div>
            </div>
        <% } else if (environment === 'test') { %>
            <div class="bg-yellow-500/20 backdrop-blur-lg rounded-xl shadow-2xl p-12 text-white border border-yellow-400/30 max-w-2xl w-full transform hover:scale-105 transition-all duration-300">
                <div class="text-center space-y-6">
                    <div class="inline-block p-3 rounded-full bg-yellow-500/30 mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                        </svg>
                    </div>
                    <div class="bg-yellow-600/30 px-4 py-2 rounded-full inline-block mb-4">
                        <span class="text-lg font-mono">NODE_ENV: <%= environment %></span>
                    </div>
                    <h1 class="text-5xl font-bold mb-4 bg-gradient-to-r from-yellow-200 to-yellow-400 text-transparent bg-clip-text">
                        Test Environment
                    </h1>
                    <p class="text-xl text-yellow-100 leading-relaxed">
                        Welcome to the test environment. This is where QA testing takes place.
                    </p>
                </div>
            </div>
        <% } else if (environment === 'production') { %>
            <div class="bg-green-500/20 backdrop-blur-lg rounded-xl shadow-2xl p-12 text-white border border-green-400/30 max-w-2xl w-full transform hover:scale-105 transition-all duration-300">
                <div class="text-center space-y-6">
                    <div class="inline-block p-3 rounded-full bg-green-500/30 mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                        </svg>
                    </div>
                    <div class="bg-green-600/30 px-4 py-2 rounded-full inline-block mb-4">
                        <span class="text-lg font-mono">NODE_ENV: <%= environment %></span>
                    </div>
                    <h1 class="text-5xl font-bold mb-4 bg-gradient-to-r from-green-200 to-green-400 text-transparent bg-clip-text">
                        Production Environment
                    </h1>
                    <p class="text-xl text-green-100 leading-relaxed">
                        Welcome to the production environment. This is the live application.
                    </p>
                </div>
            </div>
        <% } %>
    </div>

    <!-- Particles Configuration -->
    <script>
        particlesJS('particles-js', {
            particles: {
                number: {
                    value: 80,
                    density: {
                        enable: true,
                        value_area: 800
                    }
                },
                color: {
                    value: '#ffffff'
                },
                shape: {
                    type: 'circle'
                },
                opacity: {
                    value: 0.5,
                    random: false
                },
                size: {
                    value: 3,
                    random: true
                },
                line_linked: {
                    enable: true,
                    distance: 150,
                    color: '#ffffff',
                    opacity: 0.4,
                    width: 1
                },
                move: {
                    enable: true,
                    speed: 2,
                    direction: 'none',
                    random: false,
                    straight: false,
                    out_mode: 'out',
                    bounce: false
                }
            },
            interactivity: {
                detect_on: 'canvas',
                events: {
                    onhover: {
                        enable: true,
                        mode: 'repulse'
                    },
                    onclick: {
                        enable: true,
                        mode: 'push'
                    },
                    resize: true
                }
            },
            retina_detect: true
        });
    </script>
</body>
</html> 